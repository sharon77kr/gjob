<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
		  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gjob.mapper.ResumeMapper">

	<select id="selectLicense" resultType="LicenseVO">
		SELECT *
		FROM license
	</select>
	
	<select id="selectIndustry1" resultType="Industries1VO">
		SELECT *
		FROM industries1
	</select>
	
	<select id="selectIndustry2" resultType="Industries2VO">
		SELECT *
		FROM industries2
	</select>
<!-- 	
	<insert id="insertResume" parameterType="ResumePoolVO">
		INSERT INTO RESUME_POOL ( mno, i2no, ptype, intro, academic, rcareer, prize )
		VALUES ( #{ mno }, #{ i2no }, #{ ptype }, #{ intro }, #{ academic }, #{ rcareer }, #{ prize } )
	</insert>
 -->
<!-- 	<insert id="insertBoard" parameterType="CompanyVO">
		<selectKey keyProperty="cno" resultType="int" order="AFTER">
			SELECT seq_company.currval FROM DUAL
		</selectKey>
			INSERT INTO company (
				cno, cwebsite, location, 
				employees, ctype, sales, 
				cintro, ceo
			)
			VALUES (
				seq_company.nextval, #{ cwebsite, jdbcType = VARCHAR }, #{ location, jdbcType = VARCHAR },
				#{ employees, jdbcType = VARCHAR }, #{ ctype, jdbcType = VARCHAR }, #{ sales, jdbcType = VARCHAR },
				#{ cintro, jdbcType = VARCHAR }, #{ ceo, jdbcType = VARCHAR }
				
			)
	</insert> -->
<!--  -->
</mapper>